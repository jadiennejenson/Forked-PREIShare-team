import { importDep } from "../_build/common.mjs";
const createJiti = (await importDep({
	id: "jiti",
	dir: process.cwd(),
	reason: "loading config files this runtime cannot import natively",
	version: "^2.7.0"
})).createJiti;
export { createJiti };
