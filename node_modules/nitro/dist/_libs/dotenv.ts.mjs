import { importDep } from "../_build/common.mjs";
const parse = (await importDep({
	id: "dotenv",
	dir: process.cwd(),
	reason: "parsing `.env` files (`node:util.parseEnv` is not available in this runtime)"
})).parse;
export { parse };
