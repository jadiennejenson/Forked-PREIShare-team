import { importDep } from "../_build/common.mjs";
const downloadTemplate = (await importDep({
	id: "giget",
	dir: process.cwd(),
	reason: "extending the config from a remote template"
})).downloadTemplate;
export { downloadTemplate };
