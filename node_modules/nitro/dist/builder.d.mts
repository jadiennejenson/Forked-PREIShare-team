import { NitroDevApp } from "./_dev.mjs";
import { RunnerMessageListener, RunnerRPCHooks } from "env-runner";
import { IncomingMessage } from "node:http";
import { Socket } from "node:net";
import { Server, ServerOptions } from "srvx";
import { LoadConfigOptions, Nitro, NitroBuildInfo, NitroConfig, NitroOptions, TaskEvent, TaskRunnerOptions } from "nitro/types";
export declare function createNitro(config?: NitroConfig, opts?: LoadConfigOptions): Promise<Nitro>;
export declare function loadOptions(configOverrides?: NitroConfig, opts?: LoadConfigOptions): Promise<NitroOptions>;
export declare function build(nitro: Nitro): Promise<void>;
export declare function copyPublicAssets(nitro: Nitro): Promise<void>;
export declare function prepare(nitro: Nitro): Promise<void>;
export declare function getBuildInfo(root: string): Promise<{
  outputDir?: undefined;
  buildInfo?: undefined;
} | {
  outputDir: string;
  buildInfo?: NitroBuildInfo;
}>;
export declare function createDevServer(nitro: Nitro): NitroDevServer;
declare class NitroDevServer extends NitroDevApp implements RunnerRPCHooks {
  #private;
  constructor(nitro: Nitro);
  upgrade(req: IncomingMessage, socket: Socket, head: any): Promise<void>;
  listen(opts?: Partial<Omit<ServerOptions, "fetch">>): Server;
  close(): Promise<void>;
  reload(): void;
  sendMessage(message: unknown): void;
  onMessage(listener: RunnerMessageListener): void;
  offMessage(listener: RunnerMessageListener): void;
}
export declare function prerender(nitro: Nitro): Promise<void>;
/** @experimental */
export declare function runTask(taskEvent: TaskEvent, opts?: TaskRunnerOptions): Promise<{
  result: unknown;
}>;
/** @experimental */
export declare function listTasks(opts?: TaskRunnerOptions): Promise<Record<string, {
  meta: {
    description: string;
  };
}>>;