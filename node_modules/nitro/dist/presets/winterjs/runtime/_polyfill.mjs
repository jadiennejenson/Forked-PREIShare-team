


const NativeResponse = globalThis.Response;
globalThis.Response = class Response extends NativeResponse {
	constructor(body, init) {
		super(body === null ? undefined : body, init?.status == null ? {
			...init,
			status: 200
		} : init);
	}
};
