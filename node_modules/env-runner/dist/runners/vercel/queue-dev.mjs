import { resolveRuntimeDep } from "../../_chunks/common-runtime-deps.mjs";
const DEFAULT_CONSUMER_GROUP = "env-runner-vercel-dev";
const _clients = /* @__PURE__ */ new WeakMap();
const _warned = /* @__PURE__ */ new WeakSet();
let _warnedMissing = false;
let _importedSdk;
const noop = () => {};
async function registerVercelQueueConsumer(consumer) {
	const sdk = await resolveSdk(consumer.sdk);
	if (!sdk) {
		if (!_warnedMissing) {
			_warnedMissing = true;
			console.warn("[env-runner:vercel-queue] `@vercel/queue` is not installed and no `sdk` was passed. Local queue delivery is disabled.");
		}
		return noop;
	}
	if (typeof sdk.registerDevConsumer !== "function") {
		if (!_warned.has(sdk)) {
			_warned.add(sdk);
			console.warn("[env-runner:vercel-queue] The `@vercel/queue` SDK in use does not export `registerDevConsumer`. Upgrade @vercel/queue@^0.2.0 to enable local queue delivery.");
		}
		return noop;
	}
	let client = _clients.get(sdk);
	if (!client) {
		client = new sdk.QueueClient();
		_clients.set(sdk, client);
	}
	return sdk.registerDevConsumer({
		topic: consumer.topic,
		client,
		handler: consumer.handler,
		consumerGroup: consumer.consumerGroup ?? DEFAULT_CONSUMER_GROUP,
		visibilityTimeoutSeconds: consumer.visibilityTimeoutSeconds,
		retry: consumer.retry ?? (consumer.retryAfterSeconds === void 0 ? void 0 : () => ({ afterSeconds: consumer.retryAfterSeconds }))
	});
}
function resolveSdk(value) {
	if (value !== void 0) return resolveRuntimeDep({
		name: "@vercel/queue",
		option: "sdk",
		value
	});
	_importedSdk ??= resolveRuntimeDep({
		name: "@vercel/queue",
		option: "sdk"
	});
	return _importedSdk;
}
export { registerVercelQueueConsumer };
