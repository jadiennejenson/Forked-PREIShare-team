function hostEnv(extra) {
	const env = {
		...process.env,
		...extra
	};
	const stdout = process.stdout;
	if (!("FORCE_COLOR" in env) && !env.NO_COLOR && stdout?.isTTY) env.FORCE_COLOR = "1";
	if (!("COLUMNS" in env) && stdout?.columns) env.COLUMNS = String(stdout.columns);
	return env;
}
export { hostEnv };
